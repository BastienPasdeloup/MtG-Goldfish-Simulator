# Copy to .env and fill in. Only needed for LLM-backed features
# (English -> property code, card auto-implementation). The stub
# provider is used automatically when no key is present.
ANTHROPIC_API_KEY=

# Optional overrides
MTG_LLM_MODEL=claude-sonnet-5
MTG_DATA_DIR=data
