"""Remand — {1}{U} Instant. Counter target spell; put it into its owner's hand
instead of the graveyard. Then draw a card. Uncastable in a goldfish with no
opponent spell on the stack (so the draw never happens)."""
from ._common import counterspell

counterspell("Remand")
