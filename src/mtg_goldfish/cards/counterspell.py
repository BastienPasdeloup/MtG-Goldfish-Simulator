"""Counterspell — {U}{U} Instant. Counter target spell.
In a solitaire goldfish there is never an opponent's spell on the stack, so it is
uncastable by default (with instant-speed exploration it can counter your own
spell — see cards._common.counterspell)."""
from ._common import counterspell

counterspell("Counterspell")
